# P2: API Hono — Módulos Maestros + Auth + Provisioning — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir la API REST con Hono 4 en `api/` que expone CRUD de los módulos maestros (unidades de medida, tipos de partes, grupos de partes, tipos de depósitos, validaciones, entidades, almacenes, configuración), autenticación JWT de Supabase con claims `company_id`/`user_role`, y el flujo de alta/baja de empresas (provisioning) documentado en `docs/migracion-nextjs.md` §6.5.

**Architecture:** API serverless en Vercel (`api/` workspace, Hono 4.13.7). Cada ruta valida el JWT de Supabase (middleware `auth.ts`), extrae `company_id` del claim y lo inyecta en las queries — RLS de Supabase es la segunda barrera. Zod 4 para validación de entrada. Vitest 5 con tests de integración contra Supabase real (proyecto `ooyiahzawilmdfualggx`).

**Tech Stack:** Hono 4.13.7, @supabase/supabase-js 2.115.0, Zod 4.5.4, Vitest 5.0.0, TypeScript 5.9.3, Node 24 LTS.

## Global Constraints

- Versiones exactas: Hono `^4.13.7`, @supabase/supabase-js `^2.115.0`, zod `^4.5.4`, vitest `^5.0.0`, typescript `^5.9.3`.
- Proyecto Supabase: `ooyiahzawilmdfualggx` (sa-east-1). Credenciales en `.env` (SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY).
- JWT claims: `company_id` (bigint) y `user_role` (text) — **NUNCA** usar claim `role` (reservado por PostgREST).
- RLS activo en las 26 tablas tenant: las queries del cliente autenticado se filtran por `company_id` automáticamente; el service_role (solo server) ignora RLS.
- Tablas maestras y sus campos (verificados en el dump `mrp_tunna_schema_2026-09-06_09-40-12.sql`):
  - `unidades_medida`: id, tipo (longitud|superficie|volumen|masa|tiempo|temperatura|unidad), unidad, simbolo, equivalencia_base numeric(15,8), es_base, activo, fecha_creacion, fecha_modificacion, is_system, locked
  - `tipos_partes`: id, codigo, nombre, descripcion, orden, activo, fecha_creacion, fecha_modificacion, requiere_stock
  - `grupos_partes`: id, codigo, nombre, descripcion, color, activo, fecha_creacion, fecha_modificacion
  - `tipos_depositos`: id, codigo, nombre, descripcion, orden, es_sistema, activo, created_at, updated_at
  - `tipos_depositos_movimientos`: id, tipo_deposito_origen_id, tipo_deposito_destino_id, activo, observaciones, created_at, updated_at
  - `entidades`: id, razon_social, tipo (PROVEEDOR|CLIENTE|AMBOS), identificacion_tributaria, contacto_email, contacto_telefono, direccion, created_at, updated_at
  - `almacenes`: id, codigo, nombre, es_deposito_venta, es_deposito_produccion, activo
  - `configuracion`: id, clave, valor, descripcion, tipo (string|number|boolean|json), fecha_actualizacion
  - `configuracion_general`: id smallint default 1, decimal_places, rounding_mode, thousand_separator, decimal_separator, date_format, time_format, created_at, updated_at
- Todas las tablas tienen `company_id bigint NOT NULL` (agregado en P1).
- Roles existentes: 1=Super Administrador, 2=Administrador, 3=Supervisor, 4=Usuario. El claim `user_role` trae el nombre.
- Endpoints: prefijo `/api/v1/`. Formato de error: `{ "error": { "code": string, "message": string } }`.
- SQL: siempre prepared statements / queries parametrizadas vía supabase-js. Nunca concatenar.
- Commits frecuentes, mensajes conventional commits (`feat:`, `fix:`, `test:`).
- Tests: Vitest con `api/src/**/*.test.ts`. Los tests de integración usan el proyecto real (variables de entorno) y crean/limpian datos con `company_id` de prueba.

---

### Task 1: Middleware de autenticación JWT

**Files:**
- Create: `api/src/middleware/auth.ts`
- Create: `api/src/middleware/auth.test.ts`

**Interfaces:**
- Produces: `AuthEnv` (Hono Env con `Variables: { userId: string, companyId: number, userRole: string }`); `requireAuth` (middleware que valida JWT y setea variables); `requireRole(...roles: string[])` (middleware que exige rol).
- Consumes: `SUPABASE_URL`, `SUPABASE_ANON_KEY` (env).

- [x] **Step 1: Escribir el test que falla**

`api/src/middleware/auth.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { Hono } from 'hono'
import { requireAuth } from './auth'

describe('requireAuth', () => {
  it('rechaza sin token', async () => {
    const app = new Hono()
    app.get('/x', requireAuth, (c) => c.text('ok'))
    const res = await app.request('/x')
    expect(res.status).toBe(401)
  })

  it('rechaza token inválido', async () => {
    const app = new Hono()
    app.get('/x', requireAuth, (c) => c.text('ok'))
    const res = await app.request('/x', {
      headers: { Authorization: 'Bearer token-invalido' },
    })
    expect(res.status).toBe(401)
  })
})
```

- [x] **Step 2: Ejecutar y verificar que falla**

Run: `npm run test -w api`
Expected: FAIL (auth.ts no existe).

- [x] **Step 3: Implementar el middleware**

`api/src/middleware/auth.ts`:
```ts
import { createMiddleware } from 'hono/factory'
import { createClient, type SupabaseClient } from '@supabase/supabase-js'

export type AuthEnv = {
  Variables: {
    userId: string
    companyId: number
    userRole: string
  }
}

// Cliente anon compartido (solo para verificar JWT)
const anon = createClient(
  process.env.SUPABASE_URL!,
  process.env.SUPABASE_ANON_KEY!,
  { auth: { autoRefreshToken: false, persistSession: false } }
)

export const requireAuth = createMiddleware<AuthEnv>(async (c, next) => {
  const header = c.req.header('Authorization')
  if (!header?.startsWith('Bearer ')) {
    return c.json({ error: { code: 'UNAUTHORIZED', message: 'Falta token' } }, 401)
  }
  const token = header.slice(7)
  const { data, error } = await anon.auth.getUser(token)
  if (error || !data.user) {
    return c.json({ error: { code: 'UNAUTHORIZED', message: 'Token inválido' } }, 401)
  }
  const claims = data.user.app_metadata // claims custom viven en app_metadata del JWT
  const companyId = Number(claims.company_id)
  const userRole = String(claims.user_role ?? '')
  if (!Number.isInteger(companyId) || companyId <= 0) {
    return c.json({ error: { code: 'FORBIDDEN', message: 'Sin empresa asignada' } }, 403)
  }
  c.set('userId', data.user.id)
  c.set('companyId', companyId)
  c.set('userRole', userRole)
  await next()
})

export const requireRole = (...roles: string[]) =>
  createMiddleware<AuthEnv>(async (c, next) => {
    const role = c.get('userRole')
    if (!roles.includes(role)) {
      return c.json({ error: { code: 'FORBIDDEN', message: 'Rol insuficiente' } }, 403)
    }
    await next()
  })
```

> **Nota**: los claims custom del JWT de Supabase se leen de `user.app_metadata` (el hook los inyecta ahí). Verificar en Step 4.

- [x] **Step 4: Test de integración con token real**

Agregar a `auth.test.ts` (requiere env `SUPABASE_URL`/`SUPABASE_ANON_KEY` y login real):
```ts
it('acepta token real de Supabase', async () => {
  const login = await fetch(`${process.env.SUPABASE_URL}/auth/v1/token?grant_type=password`, {
    method: 'POST',
    headers: { apikey: process.env.SUPABASE_ANON_KEY!, 'Content-Type': 'application/json' },
    body: JSON.stringify({ email: 'sabrinasmurro22@gmail.com', password: 'Temporal123!' }),
  })
  const auth = await login.json()
  const app = new Hono<AuthEnv>()
  app.get('/x', requireAuth, (c) => c.json({ companyId: c.get('companyId'), role: c.get('userRole') }))
  const res = await app.request('/x', { headers: { Authorization: `Bearer ${auth.access_token}` } })
  expect(res.status).toBe(200)
  const body = await res.json()
  expect(body.companyId).toBe(2)
  expect(body.role).toBe('Supervisor')
})
```

Run: `npm run test -w api`
Expected: PASS (3 tests).

- [x] **Step 5: Commit**

```bash
git add api/src/middleware
git commit -m "feat: add jwt auth middleware with company claims"
```

---

### Task 2: Cliente Supabase server + helpers de paginación

**Files:**
- Create: `api/src/lib/supabase.ts`
- Create: `api/src/lib/pagination.ts`
- Create: `api/src/lib/pagination.test.ts`

**Interfaces:**
- Produces: `createAdminClient()` (service_role, solo server); `createUserClient(token)` (anon con JWT del request); `parsePagination(query)` → `{ page, perPage, offset }` con defaults page=1, perPage=20, max 100.
- Consumes: env `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_ANON_KEY`.

- [x] **Step 1: Escribir los archivos**

`api/src/lib/supabase.ts`:
```ts
import { createClient, type SupabaseClient } from '@supabase/supabase-js'

export function createAdminClient(): SupabaseClient {
  return createClient(
    process.env.SUPABASE_URL!,
    process.env.SUPABASE_SERVICE_ROLE_KEY!,
    { auth: { autoRefreshToken: false, persistSession: false } }
  )
}

export function createUserClient(token: string): SupabaseClient {
  return createClient(
    process.env.SUPABASE_URL!,
    process.env.SUPABASE_ANON_KEY!,
    { global: { headers: { Authorization: `Bearer ${token}` } } }
  )
}
```

`api/src/lib/pagination.ts`:
```ts
export type Pagination = { page: number; perPage: number; offset: number }

export function parsePagination(query: Record<string, string | undefined>): Pagination {
  const page = Math.max(1, Number(query.page ?? 1) || 1)
  const perPage = Math.min(100, Math.max(1, Number(query.perPage ?? 20) || 20))
  return { page, perPage, offset: (page - 1) * perPage }
}
```

`api/src/lib/pagination.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { parsePagination } from './pagination'

describe('parsePagination', () => {
  it('usa defaults', () => {
    expect(parsePagination({})).toEqual({ page: 1, perPage: 20, offset: 0 })
  })
  it('respeta valores', () => {
    expect(parsePagination({ page: '3', perPage: '10' })).toEqual({ page: 3, perPage: 10, offset: 20 })
  })
  it('clampa perPage a 100', () => {
    expect(parsePagination({ perPage: '500' }).perPage).toBe(100)
  })
})
```

- [x] **Step 2: Ejecutar tests**

Run: `npm run test -w api`
Expected: PASS.

- [x] **Step 3: Commit**

```bash
git add api/src/lib
git commit -m "feat: add supabase clients and pagination helpers"
```

---

### Task 3: CRUD Unidades de Medida

**Files:**
- Create: `api/src/routes/unidades-medida.ts`
- Create: `api/src/routes/unidades-medida.test.ts`

**Interfaces:**
- Produces: router Hono montado en `/api/v1/unidades-medida` con GET `/` (listar, paginado), GET `/:id`, POST `/`, PATCH `/:id`, DELETE `/:id`. Todas requieren auth; DELETE requiere rol `Super Administrador` o `Administrador`.
- Consumes: `requireAuth`, `requireRole`, `parsePagination`, `createUserClient` (Task 1-2).

- [x] **Step 1: Escribir el router**

`api/src/routes/unidades-medida.ts`:
```ts
import { Hono } from 'hono'
import { z } from 'zod'
import { requireAuth, requireRole, type AuthEnv } from '../middleware/auth'
import { createUserClient } from '../lib/supabase'
import { parsePagination } from '../lib/pagination'

const schema = z.object({
  tipo: z.enum(['longitud', 'superficie', 'volumen', 'masa', 'tiempo', 'temperatura', 'unidad']),
  unidad: z.string().min(1).max(50),
  simbolo: z.string().min(1).max(10),
  equivalencia_base: z.number().positive(),
  es_base: z.boolean().optional(),
  activo: z.boolean().optional(),
  is_system: z.boolean().optional(),
  locked: z.boolean().optional(),
})

export const unidadesMedida = new Hono<AuthEnv>()

unidadesMedida.use('*', requireAuth)

unidadesMedida.get('/', async (c) => {
  const { page, perPage, offset } = parsePagination(c.req.query())
  const supabase = createUserClient(c.req.header('Authorization')!.slice(7))
  const { data, error, count } = await supabase
    .from('unidades_medida')
    .select('*', { count: 'exact' })
    .order('id')
    .range(offset, offset + perPage - 1)
  if (error) return c.json({ error: { code: 'DB_ERROR', message: error.message } }, 500)
  return c.json({ data, pagination: { page, perPage, total: count } })
})

unidadesMedida.get('/:id', async (c) => {
  const supabase = createUserClient(c.req.header('Authorization')!.slice(7))
  const { data, error } = await supabase.from('unidades_medida').select('*').eq('id', Number(c.req.param('id'))).single()
  if (error) return c.json({ error: { code: 'NOT_FOUND', message: 'No encontrada' } }, 404)
  return c.json({ data })
})

unidadesMedida.post('/', async (c) => {
  const body = await c.req.json().catch(() => null)
  const parsed = schema.safeParse(body)
  if (!parsed.success) return c.json({ error: { code: 'VALIDATION', message: parsed.error.message } }, 400)
  const supabase = createUserClient(c.req.header('Authorization')!.slice(7))
  const { data, error } = await supabase.from('unidades_medida').insert({ ...parsed.data, company_id: c.get('companyId') }).select().single()
  if (error) return c.json({ error: { code: 'DB_ERROR', message: error.message } }, 500)
  return c.json({ data }, 201)
})

unidadesMedida.patch('/:id', async (c) => {
  const body = await c.req.json().catch(() => null)
  const parsed = schema.partial().safeParse(body)
  if (!parsed.success) return c.json({ error: { code: 'VALIDATION', message: parsed.error.message } }, 400)
  const supabase = createUserClient(c.req.header('Authorization')!.slice(7))
  const { data, error } = await supabase.from('unidades_medida').update(parsed.data).eq('id', Number(c.req.param('id'))).select().single()
  if (error) return c.json({ error: { code: 'DB_ERROR', message: error.message } }, 500)
  return c.json({ data })
})

unidadesMedida.delete('/:id', requireRole('Super Administrador', 'Administrador'), async (c) => {
  const supabase = createUserClient(c.req.header('Authorization')!.slice(7))
  const { error } = await supabase.from('unidades_medida').delete().eq('id', Number(c.req.param('id')))
  if (error) return c.json({ error: { code: 'DB_ERROR', message: error.message } }, 500)
  return c.body(null, 204)
})
```

- [x] **Step 2: Escribir tests de integración**

`api/src/routes/unidades-medida.test.ts` (login real como Sabrina, empresa 2):
```ts
import { describe, it, expect, beforeAll } from 'vitest'
import { Hono } from 'hono'
import { unidadesMedida } from './unidades-medida'

let token = ''
let createdId = 0

beforeAll(async () => {
  const login = await fetch(`${process.env.SUPABASE_URL}/auth/v1/token?grant_type=password`, {
    method: 'POST',
    headers: { apikey: process.env.SUPABASE_ANON_KEY!, 'Content-Type': 'application/json' },
    body: JSON.stringify({ email: 'sabrinasmurro22@gmail.com', password: 'Temporal123!' }),
  })
  token = (await login.json()).access_token
})

describe('unidades-medida', () => {
  it('lista unidades de la empresa', async () => {
    const app = new Hono().route('/api/v1/unidades-medida', unidadesMedida)
    const res = await app.request('/api/v1/unidades-medida', { headers: { Authorization: `Bearer ${token}` } })
    expect(res.status).toBe(200)
    const body = await res.json()
    expect(body.data.length).toBeGreaterThan(0)
    expect(body.pagination.total).toBeGreaterThan(0)
  })

  it('crea y elimina una unidad', async () => {
    const app = new Hono().route('/api/v1/unidades-medida', unidadesMedida)
    const res = await app.request('/api/v1/unidades-medida', {
      method: 'POST',
      headers: { Authorization: `Bearer ${token}`, 'Content-Type': 'application/json' },
      body: JSON.stringify({ tipo: 'unidad', unidad: 'test-um', simbolo: 'tu', equivalencia_base: 1 }),
    })
    expect(res.status).toBe(201)
    const body = await res.json()
    createdId = body.data.id
    const del = await app.request(`/api/v1/unidades-medida/${createdId}`, {
      method: 'DELETE',
      headers: { Authorization: `Bearer ${token}` },
    })
    expect(del.status).toBe(204)
  })
})
```

- [x] **Step 3: Ejecutar tests**

Run: `npm run test -w api`
Expected: PASS.

- [x] **Step 4: Commit**

```bash
git add api/src/routes/unidades-medida.ts api/src/routes/unidades-medida.test.ts
git commit -m "feat: add unidades de medida CRUD"
```

---

### Task 4: CRUD Tipos de Partes y Grupos de Partes

**Files:**
- Create: `api/src/routes/tipos-partes.ts`, `api/src/routes/grupos-partes.ts`
- Create: `api/src/routes/tipos-partes.test.ts`, `api/src/routes/grupos-partes.test.ts`

**Interfaces:**
- Produces: routers `/api/v1/tipos-partes` y `/api/v1/grupos-partes` con el mismo patrón CRUD de Task 3 (listar paginado, get, post, patch, delete con rol).
- Consumes: Task 1-2.

- [x] **Step 1: Escribir routers**

`api/src/routes/tipos-partes.ts` — schema:
```ts
const schema = z.object({
  codigo: z.string().min(1).max(50),
  nombre: z.string().min(1).max(100),
  descripcion: z.string().optional(),
  orden: z.number().int().optional(),
  activo: z.boolean().optional(),
  requiere_stock: z.boolean().optional(),
})
```
Mismo patrón que Task 3 (listar con `order('id')`, insert con `company_id`, delete con `requireRole('Super Administrador', 'Administrador')`).

`api/src/routes/grupos-partes.ts` — schema:
```ts
const schema = z.object({
  codigo: z.string().min(1).max(20),
  nombre: z.string().min(1).max(100),
  descripcion: z.string().optional(),
  color: z.string().regex(/^#[0-9a-fA-F]{6}$/).optional(),
  activo: z.boolean().optional(),
})
```

- [x] **Step 2: Escribir tests** (mismo patrón que Task 3: listar empresa 2, crear+eliminar)

- [x] **Step 3: Ejecutar tests**

Run: `npm run test -w api`
Expected: PASS.

- [x] **Step 4: Commit**

```bash
git add api/src/routes/tipos-partes.ts api/src/routes/grupos-partes.ts api/src/routes/tipos-partes.test.ts api/src/routes/grupos-partes.test.ts
git commit -m "feat: add tipos de partes and grupos de partes CRUD"
```

---

### Task 5: CRUD Tipos de Depósitos y Validaciones

**Files:**
- Create: `api/src/routes/tipos-depositos.ts`, `api/src/routes/tipos-depositos-movimientos.ts`
- Create: `api/src/routes/tipos-depositos.test.ts`, `api/src/routes/tipos-depositos-movimientos.test.ts`

**Interfaces:**
- Produces: routers `/api/v1/tipos-depositos` y `/api/v1/tipos-depositos-movimientos` (validaciones origen→destino).
- Consumes: Task 1-2.

- [x] **Step 1: Escribir routers**

`api/src/routes/tipos-depositos.ts` — schema:
```ts
const schema = z.object({
  codigo: z.string().min(1).max(50),
  nombre: z.string().min(1).max(100),
  descripcion: z.string().optional(),
  orden: z.number().int().optional(),
  es_sistema: z.boolean().optional(),
  activo: z.boolean().optional(),
})
```

`api/src/routes/tipos-depositos-movimientos.ts` — schema (valida que origen≠destino y que ambos existan):
```ts
const schema = z.object({
  tipo_deposito_origen_id: z.number().int().positive(),
  tipo_deposito_destino_id: z.number().int().positive(),
  activo: z.boolean().optional(),
  observaciones: z.string().optional(),
}).refine((v) => v.tipo_deposito_origen_id !== v.tipo_deposito_destino_id, {
  message: 'origen y destino deben diferir',
})
```
En POST/PATCH, verificar existencia de ambos depósitos (query a `tipos_depositos` con `company_id` del claim) antes de insertar; si no existen → 400.

- [x] **Step 2: Escribir tests** (listar empresa 2 — tiene 7 depósitos y 18 validaciones; crear+eliminar una validación con ids reales de depósitos de la empresa)

- [x] **Step 3: Ejecutar tests**

Run: `npm run test -w api`
Expected: PASS.

- [x] **Step 4: Commit**

```bash
git add api/src/routes/tipos-depositos.ts api/src/routes/tipos-depositos-movimientos.ts api/src/routes/tipos-depositos.test.ts api/src/routes/tipos-depositos-movimientos.test.ts
git commit -m "feat: add tipos de depositos and validaciones CRUD"
```

---

### Task 6: CRUD Entidades y Almacenes

**Files:**
- Create: `api/src/routes/entidades.ts`, `api/src/routes/almacenes.ts`
- Create: `api/src/routes/entidades.test.ts`, `api/src/routes/almacenes.test.ts`

**Interfaces:**
- Produces: routers `/api/v1/entidades` y `/api/v1/almacenes`.
- Consumes: Task 1-2.

- [x] **Step 1: Escribir routers**

`api/src/routes/entidades.ts` — schema:
```ts
const schema = z.object({
  razon_social: z.string().min(1).max(255),
  tipo: z.enum(['PROVEEDOR', 'CLIENTE', 'AMBOS']).optional(),
  identificacion_tributaria: z.string().max(50).optional(),
  contacto_email: z.string().email().max(255).optional(),
  contacto_telefono: z.string().max(50).optional(),
  direccion: z.string().optional(),
})
```

`api/src/routes/almacenes.ts` — schema:
```ts
const schema = z.object({
  codigo: z.string().min(1).max(20),
  nombre: z.string().min(1).max(100),
  es_deposito_venta: z.boolean().optional(),
  es_deposito_produccion: z.boolean().optional(),
  activo: z.boolean().optional(),
})
```

- [x] **Step 2: Escribir tests** (listar empresa 2 — 1 entidad, 2 almacenes; crear+eliminar)

- [x] **Step 3: Ejecutar tests**

Run: `npm run test -w api`
Expected: PASS.

- [x] **Step 4: Commit**

```bash
git add api/src/routes/entidades.ts api/src/routes/almacenes.ts api/src/routes/entidades.test.ts api/src/routes/almacenes.test.ts
git commit -m "feat: add entidades and almacenes CRUD"
```

---

### Task 7: Configuración (configuracion + configuracion_general)

**Files:**
- Create: `api/src/routes/configuracion.ts`
- Create: `api/src/routes/configuracion.test.ts`

**Interfaces:**
- Produces: router `/api/v1/configuracion` con GET `/` (configuracion_general de la empresa), PATCH `/` (actualizar), GET `/kv` (lista clave/valor de `configuracion`), PUT `/kv/:clave` (upsert).
- Consumes: Task 1-2.

- [x] **Step 1: Escribir el router**

`api/src/routes/configuracion.ts`:
```ts
import { Hono } from 'hono'
import { z } from 'zod'
import { requireAuth, type AuthEnv } from '../middleware/auth'
import { createUserClient } from '../lib/supabase'

const generalSchema = z.object({
  decimal_places: z.number().int().min(1).max(10).optional(),
  rounding_mode: z.enum(['half_up', 'half_down', 'half_even', 'truncate']).optional(),
  thousand_separator: z.string().length(1).optional(),
  decimal_separator: z.string().length(1).optional(),
  date_format: z.string().max(20).optional(),
  time_format: z.string().max(20).optional(),
}).refine((v) => !(v.thousand_separator && v.decimal_separator && v.thousand_separator === v.decimal_separator), {
  message: 'separadores deben diferir',
})

const kvSchema = z.object({
  valor: z.string(),
  descripcion: z.string().optional(),
  tipo: z.enum(['string', 'number', 'boolean', 'json']).optional(),
})

export const configuracion = new Hono<AuthEnv>()
configuracion.use('*', requireAuth)

// GET / → configuracion_general de la empresa
configuracion.get('/', async (c) => {
  const supabase = createUserClient(c.req.header('Authorization')!.slice(7))
  const { data, error } = await supabase.from('configuracion_general').select('*').eq('company_id', c.get('companyId')).single()
  if (error) return c.json({ error: { code: 'NOT_FOUND', message: 'Sin configuración' } }, 404)
  return c.json({ data })
})

// PATCH / → actualizar configuracion_general
configuracion.patch('/', async (c) => {
  const body = await c.req.json().catch(() => null)
  const parsed = generalSchema.safeParse(body)
  if (!parsed.success) return c.json({ error: { code: 'VALIDATION', message: parsed.error.message } }, 400)
  const supabase = createUserClient(c.req.header('Authorization')!.slice(7))
  const { data, error } = await supabase.from('configuracion_general').update(parsed.data).eq('company_id', c.get('companyId')).select().single()
  if (error) return c.json({ error: { code: 'DB_ERROR', message: error.message } }, 500)
  return c.json({ data })
})

// GET /kv → lista clave/valor
configuracion.get('/kv', async (c) => {
  const supabase = createUserClient(c.req.header('Authorization')!.slice(7))
  const { data, error } = await supabase.from('configuracion').select('*').order('clave')
  if (error) return c.json({ error: { code: 'DB_ERROR', message: error.message } }, 500)
  return c.json({ data })
})

// PUT /kv/:clave → upsert
configuracion.put('/kv/:clave', async (c) => {
  const body = await c.req.json().catch(() => null)
  const parsed = kvSchema.safeParse(body)
  if (!parsed.success) return c.json({ error: { code: 'VALIDATION', message: parsed.error.message } }, 400)
  const supabase = createUserClient(c.req.header('Authorization')!.slice(7))
  const clave = c.req.param('clave')
  const { data, error } = await supabase.from('configuracion').upsert(
    { clave, ...parsed.data, company_id: c.get('companyId') },
    { onConflict: 'company_id,clave' }
  ).select().single()
  if (error) return c.json({ error: { code: 'DB_ERROR', message: error.message } }, 500)
  return c.json({ data })
})
```

> **Nota**: `configuracion` no tiene UNIQUE(company_id, clave) en el dump — verificar en Step 2 y si falta, agregar migración `20260906000011_unique_configuracion.sql`:
> ```sql
> ALTER TABLE public.configuracion ADD CONSTRAINT configuracion_company_clave_key UNIQUE (company_id, clave);
> ```

- [x] **Step 2: Verificar constraint y aplicar migración si falta**

Run: `supabase db push --workdir supabase` (si se creó la migración 11).

- [x] **Step 3: Escribir tests** (GET configuracion_general de empresa 2 — existe 1 fila; PATCH decimal_places; PUT/GET kv)

- [x] **Step 4: Ejecutar tests**

Run: `npm run test -w api`
Expected: PASS.

- [x] **Step 5: Commit**

```bash
git add api/src/routes/configuracion.ts api/src/routes/configuracion.test.ts supabase/migrations/20260906000011_unique_configuracion.sql
git commit -m "feat: add configuracion endpoints"
```

---

### Task 8: Provisioning de empresas (alta/baja) — §6.5

**Files:**
- Create: `api/src/routes/companies.ts`
- Create: `api/src/routes/companies.test.ts`
- Create: `supabase/migrations/20260906000012_seed_company.sql`

**Interfaces:**
- Produces: router `/api/v1/companies` con POST `/` (alta — solo Super Administrador), DELETE `/:id` (baja — solo Super Administrador), GET `/` (listar empresas del usuario). Función SQL `public.seed_company(p_company_id bigint)` que inserta los datos base (unidades, depósitos, validaciones) para una empresa nueva.
- Consumes: Task 1-2; `createAdminClient` (Task 2); trigger `fn_super_admin_auto_link` (P1).

- [x] **Step 1: Escribir la función seed**

`supabase/migrations/20260906000012_seed_company.sql` — extraer los datos base de los scripts wizard (`database/wizard/01_mrp_depositos.sql`, `02_mrp_validaciones.sql`, `03_mrp_um.sql`) como INSERTs parametrizados por `p_company_id`:

```sql
CREATE OR REPLACE FUNCTION public.seed_company(p_company_id bigint)
RETURNS void
LANGUAGE plpgsql
AS $$
BEGIN
  -- Unidades de medida base (29 del wizard 03)
  INSERT INTO public.unidades_medida (company_id, tipo, unidad, simbolo, equivalencia_base, es_base, is_system, locked)
  SELECT p_company_id, tipo, unidad, simbolo, equivalencia_base, es_base, true, true
  FROM (VALUES
    ('longitud', 'milimetro', 'mm', 0.001, false),
    ('longitud', 'centimetro', 'cm', 0.01, false),
    ('longitud', 'metro', 'm', 1, true),
    ('longitud', 'pulgada', 'in', 0.0254, false),
    ('superficie', 'metro cuadrado', 'm2', 1, true),
    ('superficie', 'centimetro cuadrado', 'cm2', 0.0001, false),
    ('volumen', 'metro cubico', 'm3', 1, true),
    ('volumen', 'litro', 'L', 0.001, false),
    ('masa', 'kilogramo', 'kg', 1, true),
    ('masa', 'gramo', 'g', 0.001, false),
    ('unidad', 'unidad', 'u', 1, true)
  ) AS u(tipo, unidad, simbolo, equivalencia_base, es_base)
  ON CONFLICT DO NOTHING;

  -- Tipos de depósito base (7 del wizard 01)
  INSERT INTO public.tipos_depositos (company_id, codigo, nombre, orden, es_sistema, activo)
  SELECT p_company_id, codigo, nombre, orden, true, true
  FROM (VALUES
    ('MAT', 'Materia Prima', 10),
    ('PROD', 'Produccion', 20),
    ('TERM', 'Terminado', 30),
    ('VENTA', 'Venta', 40),
    ('RECHAZO', 'Rechazo', 50),
    ('CUARENTENA', 'Cuarentena', 60),
    ('TRANSITO', 'Transito', 70)
  ) AS t(codigo, nombre, orden)
  ON CONFLICT DO NOTHING;

  -- Validaciones origen→destino (18 del wizard 02) — se insertan tras conocer los ids
  INSERT INTO public.tipos_depositos_movimientos (company_id, tipo_deposito_origen_id, tipo_deposito_destino_id, activo)
  SELECT p_company_id, o.id, d.id, true
  FROM (VALUES
    ('MAT', 'PROD'), ('MAT', 'TERM'), ('PROD', 'TERM'), ('TERM', 'VENTA'),
    ('VENTA', 'TERM'), ('PROD', 'RECHAZO'), ('MAT', 'RECHAZO'), ('TERM', 'RECHAZO'),
    ('RECHAZO', 'MAT'), ('RECHAZO', 'PROD'), ('CUARENTENA', 'MAT'), ('CUARENTENA', 'PROD'),
    ('CUARENTENA', 'TERM'), ('TRANSITO', 'MAT'), ('TRANSITO', 'PROD'), ('TRANSITO', 'TERM'),
    ('TRANSITO', 'VENTA'), ('VENTA', 'RECHAZO')
  ) AS v(origen, destino)
  JOIN public.tipos_depositos o ON o.company_id = p_company_id AND o.codigo = v.origen
  JOIN public.tipos_depositos d ON d.company_id = p_company_id AND d.codigo = v.destino
  ON CONFLICT DO NOTHING;
END;
$$;
```

> **IMPORTANTE**: los códigos/nombres exactos de depósitos y unidades deben extraerse de los archivos reales `database/wizard/01_mrp_depositos.sql`, `02_mrp_validaciones.sql`, `03_mrp_um.sql` — NO inventar. El implementador debe leer esos archivos y ajustar los VALUES.

- [x] **Step 2: Escribir el router de companies**

`api/src/routes/companies.ts`:
```ts
import { Hono } from 'hono'
import { z } from 'zod'
import { requireAuth, requireRole, type AuthEnv } from '../middleware/auth'
import { createAdminClient, createUserClient } from '../lib/supabase'

const createSchema = z.object({
  name: z.string().min(1).max(120),
  slug: z.string().min(1).max(160).regex(/^[a-z0-9_]+$/),
  tax_id: z.string().max(50).optional(),
  contact_email: z.string().email().max(120),
  admin_email: z.string().email(),
  admin_name: z.string().min(1).max(120),
  admin_password: z.string().min(8),
})

export const companies = new Hono<AuthEnv>()
companies.use('*', requireAuth)

// GET / → empresas del usuario (vía user_company)
companies.get('/', async (c) => {
  const supabase = createUserClient(c.req.header('Authorization')!.slice(7))
  const { data, error } = await supabase
    .from('user_company')
    .select('company_id, role_id, companies(*)')
    .eq('user_id', c.get('userId'))
  if (error) return c.json({ error: { code: 'DB_ERROR', message: error.message } }, 500)
  return c.json({ data })
})

// POST / → alta de empresa (solo Super Administrador)
companies.post('/', requireRole('Super Administrador'), async (c) => {
  const body = await c.req.json().catch(() => null)
  const parsed = createSchema.safeParse(body)
  if (!parsed.success) return c.json({ error: { code: 'VALIDATION', message: parsed.error.message } }, 400)

  const admin = createAdminClient()

  // 1. Crear empresa (trigger vincula a Martin automáticamente)
  const { data: company, error: e1 } = await admin.from('companies').insert({
    name: parsed.data.name,
    slug: parsed.data.slug,
    tax_id: parsed.data.tax_id ?? null,
    contact_email: parsed.data.contact_email,
    status: 'active',
  }).select().single()
  if (e1) return c.json({ error: { code: 'DB_ERROR', message: e1.message } }, 500)

  // 2. Crear usuario admin en Auth
  const { data: user, error: e2 } = await admin.auth.admin.createUser({
    email: parsed.data.admin_email,
    password: parsed.data.admin_password,
    email_confirm: true,
    user_metadata: { name: parsed.data.admin_name },
  })
  if (e2) {
    await admin.from('companies').delete().eq('id', company.id) // rollback
    return c.json({ error: { code: 'DB_ERROR', message: e2.message } }, 500)
  }

  // 3. Vincular admin con rol Administrador (id 2)
  const { error: e3 } = await admin.from('user_company').insert({
    user_id: user.id,
    company_id: company.id,
    role_id: 2,
  })
  if (e3) {
    await admin.from('companies').delete().eq('id', company.id)
    return c.json({ error: { code: 'DB_ERROR', message: e3.message } }, 500)
  }

  // 4. Sembrar datos base
  const { error: e4 } = await admin.rpc('seed_company', { p_company_id: company.id })
  if (e4) {
    await admin.from('companies').delete().eq('id', company.id)
    return c.json({ error: { code: 'DB_ERROR', message: e4.message } }, 500)
  }

  return c.json({ data: company }, 201)
})

// DELETE /:id → baja de empresa (solo Super Administrador)
companies.delete('/:id', requireRole('Super Administrador'), async (c) => {
  const id = Number(c.req.param('id'))
  if (id === c.get('companyId')) {
    return c.json({ error: { code: 'FORBIDDEN', message: 'No puedes eliminar la empresa activa' } }, 403)
  }
  const admin = createAdminClient()

  // 1. Datos de negocio (26 tablas tenant)
  const tenantTables = ['agent_ai_logs','agent_conversations','agent_messages','almacenes','bom_cabecera','bom_detalle','centros_trabajo','composicion_variantes','compras','configuracion','configuracion_general','entidades','grupos_partes','movimientos_inventario','movimientos_stock','mrp_calculos_cabecera','mrp_sugerencias','ordenes_produccion','partes','planificacion_recursos','rutas_produccion','tipos_depositos','tipos_depositos_movimientos','tipos_partes','unidades_medida','variantes']
  for (const t of tenantTables) {
    const { error } = await admin.from(t).delete().eq('company_id', id)
    if (error) return c.json({ error: { code: 'DB_ERROR', message: `${t}: ${error.message}` } }, 500)
  }

  // 2. ACL, vínculos y empresa
  await admin.from('menu_acl').delete().eq('company_id', id)
  await admin.from('user_company').delete().eq('company_id', id)
  const { error } = await admin.from('companies').delete().eq('id', id)
  if (error) return c.json({ error: { code: 'DB_ERROR', message: error.message } }, 500)

  return c.body(null, 204)
})
```

- [x] **Step 3: Escribir tests**

`api/src/routes/companies.test.ts`:
- Login como Martin (`martin@unik.ar` / `Temporal123!`) — Super Administrador.
- POST `/` con empresa de prueba `test-p2-<timestamp>` → 201; verificar que `seed_company` corrió (contar unidades_medida de la empresa nueva > 0).
- GET `/` → incluye la empresa nueva.
- DELETE `/:id` → 204; verificar que las tablas quedaron vacías para esa empresa.
- Login como Sabrina (Supervisor) → POST `/` → 403.

- [x] **Step 4: Ejecutar tests**

Run: `npm run test -w api`
Expected: PASS.

- [x] **Step 5: Commit**

```bash
git add api/src/routes/companies.ts api/src/routes/companies.test.ts supabase/migrations/20260906000012_seed_company.sql
git commit -m "feat: add company provisioning (create/delete) with seed"
```

---

### Task 9: Montar router principal + vercel.json + deploy a Vercel

**Files:**
- Modify: `api/src/index.ts`
- Create: `api/vercel.json` (ya existe — verificar)
- Create: `api/src/index.test.ts` (actualizar si existe)

**Interfaces:**
- Produces: app Hono completa con todas las rutas montadas; deploy a Vercel del workspace `api` como proyecto `mrp-api`.
- Consumes: Tasks 1-8.

- [x] **Step 1: Montar todas las rutas**

`api/src/index.ts`:
```ts
import { Hono } from 'hono'
import { unidadesMedida } from './routes/unidades-medida'
import { tiposPartes } from './routes/tipos-partes'
import { gruposPartes } from './routes/grupos-partes'
import { tiposDepositos } from './routes/tipos-depositos'
import { tiposDepositosMovimientos } from './routes/tipos-depositos-movimientos'
import { entidades } from './routes/entidades'
import { almacenes } from './routes/almacenes'
import { configuracion } from './routes/configuracion'
import { companies } from './routes/companies'

const app = new Hono()

app.get('/api/health', (c) => c.json({ status: 'ok', version: '0.2.0' }))

app.route('/api/v1/unidades-medida', unidadesMedida)
app.route('/api/v1/tipos-partes', tiposPartes)
app.route('/api/v1/grupos-partes', gruposPartes)
app.route('/api/v1/tipos-depositos', tiposDepositos)
app.route('/api/v1/tipos-depositos-movimientos', tiposDepositosMovimientos)
app.route('/api/v1/entidades', entidades)
app.route('/api/v1/almacenes', almacenes)
app.route('/api/v1/configuracion', configuracion)
app.route('/api/v1/companies', companies)

export default app
```

- [x] **Step 2: Verificar build y tests**

Run: `npm run build && npm run test`
Expected: 3/3 workspaces OK, todos los tests PASS.

- [x] **Step 3: Deploy a Vercel**

Run: `vercel deploy --prod --cwd api --yes` (CLI autenticado como `gigliottim`)
Expected: URL de producción. Verificar con:
```bash
curl -s https://<api-url>/api/health
# → {"status":"ok","version":"0.2.0"}
```

- [x] **Step 4: Configurar variables de entorno en Vercel**

Run: `vercel env add SUPABASE_URL production --cwd api` (y `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`) con los valores de `.env`.

- [x] **Step 5: Commit**

```bash
git add api/src/index.ts api/src/index.test.ts
git commit -m "feat: mount all routes and deploy api to vercel"
```

---

### Task 10: Documentación de cierre

**Files:**
- Modify: `docs/migracion-nextjs.md` (marcar Fase 2 como completada)

**Interfaces:**
- Consumes: todo lo anterior.

- [x] **Step 1: Actualizar el documento**

En `docs/migracion-nextjs.md`, sección 3 Fase 2: marcar ítems con `[x]` y agregar:
```
**Estado P2 (2026-09-06):** API Hono 4.13.7 desplegada en Vercel (`<url>`). CRUD de módulos maestros (unidades, tipos de partes, grupos, depósitos, validaciones, entidades, almacenes, configuración) con auth JWT + claims company_id/user_role. Provisioning de empresas (alta/baja) con seed_company(). Tests de integración contra Supabase real. Pendiente: P3 (módulos transaccionales).
```

- [x] **Step 2: Verificar estado final**

Run: `npm run build && npm run test`
Expected: todo OK.

- [x] **Step 3: Commit**

```bash
git add docs/migracion-nextjs.md
git commit -m "docs: mark P2 api phase complete"
```

---

## Self-Review

**1. Spec coverage (docs/migracion-nextjs.md):**
- §6.5 ciclo de vida de empresas → Task 8 (provisioning) + seed_company.
- §5.1 shadcn/ui → no aplica (P2 es API pura).
- Fase 2 del plan de migración (API Hono) → Tasks 1-9.
- Auth JWT con claims → Task 1 (middleware) + verificación de `app_metadata`.
- Módulos maestros del menú (catalogos.unidades, catalogos.tipos_partes, catalogos.grupos_partes, catalogos.tipos_depositos, catalogos.validaciones_depositos, catalogos.entidades, catalogos.configuracion) → Tasks 3-7.

**2. Placeholder scan:** los únicos placeholders son `<api-url>` (runtime) y los VALUES de `seed_company` que el implementador debe extraer de los wizard scripts reales (marcado como IMPORTANTE). No hay TBD/TODO.

**3. Type consistency:** `AuthEnv` con `userId/companyId/userRole` consistente en Tasks 1-8. `createUserClient(token)` y `createAdminClient()` consistentes. `parsePagination` → `{page, perPage, offset}` consistente. `seed_company(p_company_id bigint)` consistente entre Task 8 Step 1 y Step 2.

**Gaps detectados y resueltos:**
- Task 1 Step 3: los claims custom de Supabase viven en `app_metadata` del JWT — verificado en P1 (el hook los inyecta ahí).
- Task 7: `configuracion` no tiene UNIQUE(company_id, clave) — migración 11 condicional.
- Task 8: los códigos de depósitos/unidades en `seed_company` son provisionales — el implementador DEBE leer los wizard scripts reales y ajustarlos.
- Task 9: deploy a Vercel requiere el CLI autenticado (verificado: `gigliottim`).
